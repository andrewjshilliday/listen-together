import React from 'react';
import { ChatIcon, ChatBox } from '../../core';
import styled from 'styled-components';

const Chat: React.FC = (props: any) => {  
  return (
    <>
      <ChatIcon />
      <ChatBox />
    </>
  );
}

export default Chat;
